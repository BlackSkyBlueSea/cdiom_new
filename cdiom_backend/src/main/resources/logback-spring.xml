<?xml version="1.0" encoding="UTF-8"?>
<configuration>
    <!-- 引入Spring Boot默认配置 -->
    <include resource="org/springframework/boot/logging/logback/defaults.xml"/>
    
    <!-- 控制台输出 -->
    <appender name="CONSOLE" class="ch.qos.logback.core.ConsoleAppender">
        <encoder>
            <pattern>%d{yyyy/MM/dd HH:mm:ss.SSS} [%thread] %-5level %logger{50} - %msg%n</pattern>
            <charset>UTF-8</charset>
        </encoder>
    </appender>
    
    <!-- WebSocket日志推送 -->
    <appender name="WEBSOCKET" class="com.cdiom.backend.config.WebSocketLogAppender">
        <applicationName>cdiom-backend</applicationName>
    </appender>
    
    <!-- 文件输出（可选） -->
    <appender name="FILE" class="ch.qos.logback.core.rolling.RollingFileAppender">
        <file>logs/cdiom-backend.log</file>
        <encoder>
            <pattern>%d{yyyy/MM/dd HH:mm:ss.SSS} [%thread] %-5level %logger{50} - %msg%n</pattern>
            <charset>UTF-8</charset>
        </encoder>
        <rollingPolicy class="ch.qos.logback.core.rolling.SizeAndTimeBasedRollingPolicy">
            <fileNamePattern>logs/cdiom-backend-%d{yyyy-MM-dd}.%i.log</fileNamePattern>
            <maxFileSize>100MB</maxFileSize>
            <maxHistory>30</maxHistory>
            <totalSizeCap>3GB</totalSizeCap>
        </rollingPolicy>
    </appender>
    
    <!-- 根日志级别 -->
    <root level="INFO">
        <appender-ref ref="CONSOLE"/>
        <appender-ref ref="WEBSOCKET"/>
        <appender-ref ref="FILE"/>
    </root>
    
    <!-- 应用包日志级别 -->
    <logger name="com.cdiom.backend" level="DEBUG"/>
    
    <!-- Spring框架日志级别 -->
    <logger name="org.springframework" level="INFO"/>
    
    <!-- 数据库日志级别 -->
    <logger name="com.cdiom.backend.mapper" level="DEBUG"/>
</configuration>

